# منصة المستر — أ. محمد رمضان

React/Vite version prepared for GitHub Pages.

## Important
This version uses browser localStorage for the unlock/progress demo. It is NOT a secure production backend.
The registration form still sends submitted data to the Google Apps Script endpoint already present in the original source.

For a real production platform, replace client-side unlock logic with authenticated server-side permissions and a secure database.
